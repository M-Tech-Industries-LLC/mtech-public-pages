# Legacy Afon Theme Registry

This directory is retained for legacy GitHub Pages theme consumers.

The canonical active Afon theme catalog is `/afon/themes/catalog.json`, with per-theme install metadata and manifests under `/afon/themes/{themeId}/`. Keep this legacy registry mirrored only while older clients still read it.
